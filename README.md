# iSnippets - 智能摘录工具

使用现代技术栈构建的Chrome浏览器扩展，用于智能网页文本摘录和管理。

## ✨ 核心功能

- 🚀 **自动保存**：选中网页文本后按 `Ctrl+C` 自动保存
- 🎨 **智能高亮**：保存后立即在网页上高亮显示
- 🔄 **持久高亮**：页面刷新后仍保持高亮状态
- 📏 **字符限制**：自动截取前144个字符
- 🌊 **瀑布流布局**：美观的3列瀑布流展示
- 🖱️ **点击跳转**：点击卡片直接跳转到原网页
- 🌍 **多语言支持**：中文/英文/日文界面切换
- 🎨 **现代化UI**：基于Tailwind CSS的平面化设计

## 🛠️ 技术栈

### 前端框架
- **React 18** - 现代化的UI框架
- **TypeScript** - 类型安全的JavaScript
- **Tailwind CSS** - 实用优先的CSS框架

### 构建工具
- **Bun** - 超快的JavaScript运行时和包管理器
- **Custom Bundler** - 轻量级React打包器

### 扩展架构
- **Chrome Extension Manifest V3**
- **Service Worker** - 后台数据处理
- **Content Script** - 网页内容交互
- **IndexedDB** - 本地数据存储

## 📁 项目结构

```
iSnippets/
├── src/                          # TypeScript 源代码
│   ├── components/               # React 组件
│   │   ├── ClipCard.tsx         # 摘录卡片组件
│   │   ├── SearchBar.tsx        # 搜索栏组件
│   │   └── EmptyState.tsx       # 空状态组件
│   ├── hooks/                   # 自定义 React hooks
│   │   ├── useChromeMessaging.ts # Chrome 消息通信
│   │   └── useI18n.ts          # 国际化
│   ├── pages/                   # 页面组件
│   │   ├── Library.tsx          # 主库页面
│   │   └── index.ts            # 应用入口
│   ├── styles/                  # 样式文件
│   │   └── main.css            # Tailwind 主样式
│   └── types/                   # TypeScript 类型定义
│       └── index.ts
├── dist/                        # 构建输出目录
├── manifest.json                # Chrome 扩展配置
├── content.js                   # 内容脚本（保持原样）
├── background.js                # 后台脚本（简化版本）
├── popup.html/popup.js          # 弹出窗口（简化版本）
├── settings.html/settings.js    # 设置页面（更新版本）
├── package.json                 # 项目配置
├── tsconfig.json               # TypeScript 配置
├── tailwind.config.js          # Tailwind 配置
└── bundler.js                  # 自定义打包器
```

## 🚀 开发指南

### 环境要求

- **Bun** >= 1.0.0
- **Node.js** >= 16.0.0 (用于某些工具兼容性)
- **Chrome** >= 88 (支持Manifest V3)

### 安装依赖

```bash
bun install
```

### 开发模式

```bash
# 构建开发版本并启动监听模式
bun run dev
```

### 生产构建

```bash
# 构建生产版本
bun run build
```

### 单独构建步骤

```bash
# 仅构建 TypeScript
bun run build:ts

# 仅打包 React 组件
bun run bundle

# 仅构建 CSS
bun run build:css

# 代码检查
bun run lint

# 类型检查
bun run type-check
```

## 📦 安装扩展

1. 运行构建命令：
   ```bash
   bun run build
   ```

2. 打开 Chrome 浏览器，访问 `chrome://extensions/`

3. 开启"开发者模式"

4. 点击"加载已解压的扩展程序"

5. 选择 `iSnippets/dist` 目录

## 🎯 使用方法

1. **安装扩展**后，在任意网页上选中文本
2. 按 `Ctrl+C` (或 `Cmd+C` on Mac) 自动保存
3. 选中文本会立即高亮显示
4. 点击扩展图标查看已保存的摘录
5. 在摘录库中可以搜索、切换语言
6. 点击任意摘录卡片跳转到原网页

## 🌍 多语言支持

扩展支持三种语言：

- **中文 (zh-CN)** - 默认语言
- **English (en)** - 英文界面
- **日本語 (ja)** - 日文界面

在设置页面或摘录库中可以切换语言。

## 🔧 自定义配置

### Tailwind CSS 主题

在 `tailwind.config.js` 中自定义颜色和样式：

```javascript
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          500: '#3b82f6',
          // ... 更多颜色
        }
      }
    }
  }
}
```

### TypeScript 类型

在 `src/types/index.ts` 中定义和扩展类型：

```typescript
export interface ClipCard {
  id: string;
  clipText: string;
  url: string;
  domain: string;
  createdAt: number;
  // ... 更多字段
}
```

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- [React](https://reactjs.org/) - 用于构建用户界面的 JavaScript 库
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先的 CSS 框架
- [Bun](https://bun.sh/) - 快速的 JavaScript 运行时
- [TypeScript](https://www.typescriptlang.org/) - JavaScript 的类型化超集
- [Chrome Extensions](https://developer.chrome.com/docs/extensions/) - 浏览器扩展平台

---

**注意**: 这是一个现代化的重构版本，保持了原有 iSnippets 扩展的所有核心功能，同时采用了最新的前端开发技术和最佳实践。
